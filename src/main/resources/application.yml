spring:
  profiles:
    include: github

  h2:
    console:
      enabled: true

  datasource:
    hikari:
      jdbc-url: "jdbc:h2:mem:testdb"
      username: "sa"
      driver-class-name: "org.h2.Driver"
